# Frontend Environment Configuration

# API Configuration
# Primary API URL - this will be used if set
VITE_API_BASE_URL=

# Development API URL (used when running locally)
VITE_DEV_API_URL=http://127.0.0.1:8000

# Production API URL (used as fallback for production)
# Production API URL (used as fallback for production)
VITE_PROD_API_URL=https://your-backend-domain.vercel.app

# Application Environment
VITE_APP_ENV=development

# App Configuration
VITE_APP_NAME=HRMS Dashboard
VITE_APP_VERSION=1.0.0

# Feature Flags
VITE_ENABLE_DEBUG=false
VITE_ENABLE_ANALYTICS=false
